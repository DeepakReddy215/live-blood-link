# NPM Configuration for Render Deployment
# This ensures smooth builds on Render platform

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true

# Disable package-lock generation (optional, use if needed)
# package-lock=false

# Set registry (optional, for faster installs)
registry=https://registry.npmjs.org/

# Enable strict SSL
strict-ssl=true
